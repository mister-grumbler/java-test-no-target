server:
  port: 8080
  contextPath: /
---
spring:
  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL5Dialect
  profiles: default
  datasource:
    name: seminardb
    url: jdbc:mysql://localhost:3306/seminars
    username: courseadm
    password: MyCourseSQL
    driverClassName: com.mysql.jdbc.Driver
